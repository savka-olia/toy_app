<%= form_with(model: micropost) do |form| %>
  <% if micropost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(micropost.errors.count, "error") %> prohibited this micropost from being saved:</h2>

      <ul>
        <% micropost.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <form>
    <div class="form-row align-items-center ml-2">
      <div class="field col-auto">
        <%= form.label :content %>
        <%= form.text_area :content, class:"form-control mb-2 mr-2" %>
      </div>

      <div class="field">
        <%= form.label :user_id %>
        <%= form.number_field :user_id, class:"form-control mb-2" %>
      </div>

      <div class="actions col-auto">
        <%= form.submit class:"btn btn-primary mb-2" %>
      </div>
    </div>
  </form>
<% end %>
